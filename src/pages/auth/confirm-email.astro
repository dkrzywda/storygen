---
import Layout from "@/layouts/Layout.astro";

const isAutoConfirmed = import.meta.env.DEV;

const content = isAutoConfirmed
  ? {
      emoji: "✅",
      heading: "Rejestracja zakończona",
      description: "Konto zostało utworzone. Możesz się teraz zalogować.",
      linkText: "Przejdź do logowania",
    }
  : {
      emoji: "📧",
      heading: "Sprawdź skrzynkę",
      description: "Wysłaliśmy link potwierdzający na Twój adres e-mail. Kliknij go, aby aktywować konto.",
      linkText: "Powrót do logowania",
    };
---

<Layout title={content.heading}>
  <div class="bg-app flex min-h-screen items-center justify-center p-4">
    <div class="border-hairline bg-panel text-ink w-full max-w-sm rounded-2xl border p-8 text-center shadow-sm">
      <div class="mb-4 text-5xl">{content.emoji}</div>
      <h1 class="text-ink mb-3 text-2xl font-bold">
        {content.heading}
      </h1>
      <p class="text-ink-muted mb-6">{content.description}</p>
      <a href="/auth/signin" class="text-brand text-sm hover:underline">
        {content.linkText}
      </a>
    </div>
  </div>
</Layout>
